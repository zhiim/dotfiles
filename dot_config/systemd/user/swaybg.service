[Unit]
PartOf=graphical-session.target
After=graphical-session.target
Requisite=graphical-session.target

[Service]
ExecStart=/usr/bin/bash -c '/usr/bin/swaybg -i $(cat %h/.cache/current_wallpaper) -m fill'
Restart=on-failure
